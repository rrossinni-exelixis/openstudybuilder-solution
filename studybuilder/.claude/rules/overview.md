# Project Overview

StudyBuilder is a Vue 3 frontend application for clinical study design and management.
It uses Vite as the build tool, Vuetify 3 for UI components, and Pinia for state management.
The application connects to a backend API and supports OAuth/OIDC authentication with role-based access control.


