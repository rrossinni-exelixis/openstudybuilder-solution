This is a "Hellow world" example of an OpenStudyBuilder UI extension.

Note: To enable this extension, either set the `hello_extension` feature flag to `true`, or comment out these lines in `router/index.js` and `stores/app.js` files:
```
featureFlag: 'hello_extension', // Optional: show only when feature flag is enabled
```

